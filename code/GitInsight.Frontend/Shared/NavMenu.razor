<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">GitInsight</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                Choose Repo
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="CommitsTotal" Match="NavLinkMatch.All">
                Total Commits
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="CommitsAuthor" Match="NavLinkMatch.All">
                Commits per Author
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="CommitsForks" Match="NavLinkMatch.All">
                Repo Forks
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}